<section class="auth-center">
  <h1 class="page-title"><%= title %></h1>

  <%- messages() %>

  <% if (errors) { %>
    <ul class="notice">
    <% errors.array && errors.array().forEach(error => { %>
      <li><%= error.msg %></li>
    <% }) %>
    </ul>
  <% } %>

  <section class="auth-card">
    <% if (typeof accountData !== 'undefined') { %>
      <h2>Welcome <%= accountData.account_firstname %></h2>

      <% if (accountData.account_type === 'Client') { %>
        <section class="account-management-links">
          <a href="/account/update/<%= accountData.account_id %>" class="account-link" title="Update your account information">Update Account Information</a>
          <a href="/reviews/user-reviews" class="btn" title="View and manage your reviews" style="background: linear-gradient(135deg, #7c3aed 0%, #a855f7 100%);">My Reviews</a>
        </section>
      <% } else if (accountData.account_type === 'Employee' || accountData.account_type === 'Admin') { %>
        <section class="account-management-links">
          <a href="/inv/" class="inventory-link" title="Manage inventory">Access Inventory Management</a>
          <a href="/account/update/<%= accountData.account_id %>" class="account-link" title="Update your account information">Update Account Information</a>
          <a href="/reviews/user-reviews" class="btn" title="View and manage your reviews" style="background: linear-gradient(135deg, #7c3aed 0%, #a855f7 100%); grid-column: 1 / -1; margin-top: 1rem;">My Reviews</a>
        </section>
      <% } %>
    <% } else { %>
      <p>You're logged in.</p>
    <% } %>
  </section>
</section>
